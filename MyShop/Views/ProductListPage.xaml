<Page
    x:Class="MyShop.Views.ProductListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:models="using:MyShop.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Danh Sách S?n Ph?m" 
                          Style="{StaticResource TitleTextBlockStyle}"
                          FontWeight="Bold"/>
                <TextBlock Style="{StaticResource BodyTextBlockStyle}"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                          Margin="0,4,0,0">
                    <Run Text="T?ng s?:"/>
                    <Run Text="{x:Bind ViewModel.ProductCount, Mode=OneWay}"/>
                    <Run Text="s?n ph?m"/>
                </TextBlock>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                <Button Command="{x:Bind ViewModel.RefreshCommand}"
                       ToolTipService.ToolTip="Làm m?i danh sách">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                        <TextBlock Text="Làm m?i"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Error InfoBar -->
        <InfoBar Grid.Row="1"
                 IsOpen="{x:Bind ViewModel.HasError, Mode=OneWay}"
                 Severity="Error"
                 Title="L?i"
                 Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                 IsClosable="True"
                 Margin="0,0,0,16"/>

        <!-- DataGrid -->
        <Grid Grid.Row="2">
            <!-- Progress Ring -->
            <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                         Width="60"
                         Height="60"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"/>

            <!-- DataGrid -->
            <controls:DataGrid x:Name="ProductDataGrid"
                              ItemsSource="{x:Bind ViewModel.Products, Mode=OneWay}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              AlternatingRowBackground="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                              CanUserResizeColumns="True"
                              CanUserSortColumns="True"
                              SelectionMode="Single"
                              Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityInverseConverter}}">
                
                <controls:DataGrid.Columns>
                    <!-- ID Column -->
                    <controls:DataGridTextColumn Header="Mã SP"
                                                Binding="{Binding Id}"
                                                Width="80"/>

                    <!-- Product Name Column -->
                    <controls:DataGridTextColumn Header="Tên S?n Ph?m"
                                                Binding="{Binding Name}"
                                                Width="2*"/>

                    <!-- Category Column -->
                    <controls:DataGridTextColumn Header="Lo?i"
                                                Binding="{Binding Category}"
                                                Width="150"/>

                    <!-- Price Column -->
                    <controls:DataGridTemplateColumn Header="Giá (VN?)"
                                                    Width="150">
                        <controls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate x:DataType="models:Product">
                                <TextBlock Text="{x:Bind FormattedPrice}"
                                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                          FontWeight="SemiBold"
                                          VerticalAlignment="Center"/>
                            </DataTemplate>
                        </controls:DataGridTemplateColumn.CellTemplate>
                    </controls:DataGridTemplateColumn>

                    <!-- Stock Column -->
                    <controls:DataGridTemplateColumn Header="T?n Kho"
                                                    Width="120">
                        <controls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate x:DataType="models:Product">
                                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <Ellipse Width="8" Height="8" Fill="{x:Bind StockColor}"/>
                                    <TextBlock Text="{x:Bind Stock}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </controls:DataGridTemplateColumn.CellTemplate>
                    </controls:DataGridTemplateColumn>

                    <!-- Description Column -->
                    <controls:DataGridTextColumn Header="Mô T?"
                                                Binding="{Binding Description}"
                                                Width="3*"/>
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Grid>
    </Grid>
</Page>
