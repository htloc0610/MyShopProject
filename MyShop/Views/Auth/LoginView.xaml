<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShop.Views.Auth.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.Views.Auth"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MyShop.ViewModels.Auth"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*" />
            <ColumnDefinition Width="*" MinWidth="400"/>
        </Grid.ColumnDefinitions>

        <!-- Column 0: Branding & Gradient -->
        <Grid Grid.Column="0">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#8E2DE2" Offset="0"/>
                    <GradientStop Color="#F27121" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
            
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="16">
                <TextBlock Text="Welcome Back!" 
                           Foreground="White"
                           FontSize="48" 
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Manage your shop efficiently." 
                           Foreground="#E5E5E5"
                           FontSize="20"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Column 1: Auth Forms -->
        <Grid Grid.Column="1" Background="{ThemeResource LayerFillColorDefaultBrush}" Padding="48">
            <Grid VerticalAlignment="Center" MaxWidth="400">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Header -->
                    <RowDefinition Height="Auto"/> <!-- Forms -->
                    <RowDefinition Height="Auto"/> <!-- Toggle Area -->
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,32">
                    <TextBlock x:Name="HeaderTitle" 
                               Text="{x:Bind ViewModel.IsLoginMode, Mode=OneWay, Converter={StaticResource ModeToTitleConverter}}" 
                               Style="{StaticResource TitleTextBlockStyle}"/>
                </StackPanel>

                <!-- Input Fields -->
                <StackPanel Grid.Row="1" Spacing="16">
                    <InfoBar IsOpen="{x:Bind ViewModel.ErrorMessage, Mode=OneWay, Converter={StaticResource StringIsNotEmptyToBoolConverter}}"
                             Severity="Error"
                             Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                             IsClosable="False"/>

                    <TextBox Header="Email" 
                             PlaceholderText="name@example.com"
                             Text="{x:Bind ViewModel.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    
                    <TextBox Header="Shop Name" 
                             x:Name="ShopNameInput"
                             PlaceholderText="My Awesome Shop"
                             Text="{x:Bind ViewModel.ShopName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Visibility="{x:Bind ViewModel.IsLoginMode, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>

                    <PasswordBox Header="Password" 
                                 Password="{x:Bind ViewModel.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                    <CheckBox x:Name="RememberMeCheck"
                              Content="Remember Me"
                              IsChecked="{x:Bind ViewModel.RememberMe, Mode=TwoWay}"
                              Visibility="{x:Bind ViewModel.IsLoginMode, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <Button Content="{x:Bind ViewModel.IsLoginMode, Mode=OneWay, Converter={StaticResource ModeToButtonTextConverter}}" 
                            HorizontalAlignment="Stretch"
                            Command="{x:Bind ViewModel.SubmitCommand}"
                            Style="{StaticResource AccentButtonStyle}"
                            Margin="0,8,0,0"/>
                </StackPanel>

                <!-- Toggle Mode -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,24,0,0" Spacing="4">
                    <TextBlock x:Name="ToggleText" 
                               Text="{x:Bind ViewModel.IsLoginMode, Mode=OneWay, Converter={StaticResource ModeToToggleTextConverter}}" 
                               VerticalAlignment="Center"/>
                    <HyperlinkButton x:Name="ToggleAction" 
                                     Content="{x:Bind ViewModel.IsLoginMode, Mode=OneWay, Converter={StaticResource ModeToToggleActionConverter}}" 
                                     Command="{x:Bind ViewModel.ToggleModeCommand}"/>
                </StackPanel>
            </Grid>
        </Grid>
        
        <!-- Loading Overlay -->
        <Grid Grid.ColumnSpan="2" 
              Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
              Background="#80000000">
             <ProgressRing IsActive="True" Width="64" Height="64" Foreground="White"/>
        </Grid>
    </Grid>

    <Page.Resources>
        <!-- Simple converters for binding -->
        <local:ModeToButtonTextConverter x:Key="ModeToButtonTextConverter"/>
        <local:StringIsNotEmptyToBoolConverter x:Key="StringIsNotEmptyToBoolConverter"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <local:ModeToTitleConverter x:Key="ModeToTitleConverter"/>
        <local:ModeToToggleTextConverter x:Key="ModeToToggleTextConverter"/>
        <local:ModeToToggleActionConverter x:Key="ModeToToggleActionConverter"/>
    </Page.Resources>
</Page>
